services:
  crawler:
    build: .
    ports:
      - "8081:8080"
    environment:
      - STORAGE_TYPE=local
      - OUTPUT_DIR=/app/crawled_pages
      - MAX_CONCURRENT_CRAWLS=3
      - PORT=8080
    volumes:
      - ./crawled_pages:/app/crawled_pages
